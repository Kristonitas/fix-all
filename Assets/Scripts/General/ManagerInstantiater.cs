using System.Collections;
using System.Collections.Generic;
using UnityEngine;

        //instance launches only when it is called as Manager.Method() first time!
public class ManagerInstantiater : MonoBehaviour
{
    void Start()
    {
        //GhostManager.TriggerInstantiate();
        //EventCoordinator.TriggerEvent(EventName.System.SceneLoaded(), GameMessage.Write());
    }
}
